uvicorn predict_api:app --reload
uvicorn predict_api:app --host 0.0.0.0 --port 8000 or python -m uvicorn predict_api:app --host 0.0.0.0 --port 8000
jupyter notebook or python -m notebook
php artisan tasks:send-due-notifications
